<% if current_user.has_joined(room) %>
    <%= link_to  'leave' , leave_room_path(room), method: :delete %>
    
  
<% else %>
  <%= link_to "join", join_room_path(room), method: :post  %>
  
<% end %>